# TsFlowy 更新说明

> 版本号权威源：`src-tauri/tauri.conf.json` 的 `version` 字段（与 `package.json#version` 同步）
> 安装包命名：`TsFlowy_{version}_x64-setup.exe`（Tauri NSIS 默认命名）

---

## v0.7.1 — 2026-09-19

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.7.1_x64-setup.exe`

### 修复

- **编辑器·Mermaid**：编辑图表代码时滚动条不再反复闪现——mermaid 渲染改在固定于视口外的离屏宿主元素中进行（临时渲染元素不再进入 body 正常文档流撑高文档）；编辑态跳过渲染，退出编辑时才渲染一次

### 验证

| 命令           | 结果           |
| -------------- | -------------- |
| `tsc --noEmit` | exit 0         |
| `vitest run`   | 177/177 passed |
| `eslint`       | 0 errors       |

---

## v0.7.0 — 2026-09-19

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.7.0_x64-setup.exe`

### 修复

- **页面树**：二级文档无法拖回一级目录——拖拽祖先校验参数顺序写反，把"拖到父级行"误判为环而拒绝；现仅拒绝拖入自己的后代节点
- **编辑器·附件**：表格单元格内的附件卡片宽度不再超出单元格（受列宽约束，最宽 420px）
- **编辑器·Mermaid**：语法错误提示不再泄漏显示到整个应用界面之外——渲染前先做语法预校验（错误显示在图表块内），并清理 mermaid 在 body 末尾残留的临时元素
- **编辑器·图集**：多图相册添加图片/输入图片说明后页面不再跳到底部
- **编辑器·斜杠菜单**：Mermaid 菜单项提示显示为两行——快捷词精简为单行 `mermaid`，"流程图/图表"等搜索词移入隐藏匹配字段，搜索能力不变

### 验证

| 命令           | 结果           |
| -------------- | -------------- |
| `tsc --noEmit` | exit 0         |
| `vitest run`   | 177/177 passed |
| `eslint`       | 0 errors       |

---

## v0.6.9 — 2026-09-19

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.6.9_x64-setup.exe`

### 新增

**图片宽度调节**

- 点击选中图片后右下角出现拖拽手柄，左右拖动调整显示宽度（20%–100%）
- 宽度持久化在文档中；导出/粘贴 Markdown 以 `![alt](src{width=60%})` 后缀格式往返保留

**表格单元格插入图片/附件/表情**

- 表格内斜杠菜单放行图片、附件、表情符号三类块（其余复杂块仍禁止嵌套）
- 导出 Markdown 时表格单元格内的图片等块级内容按内联语法渲染，多块间以 `<br>` 分隔，不再丢失

**Mermaid 图表块**

- 斜杠菜单插入 Mermaid 图表，支持流程图/时序图等，双击进入代码编辑，失焦重新渲染
- Markdown 围栏 ```mermaid 导入识别，导出还原围栏语法

**数据表公式字段**

- 新增「公式」字段类型：表达式支持 + - * / 与括号，`{字段名}` 引用同行字段实时计算（如 `{单价} * {数量}`）
- 字段设置中可视化编辑表达式，点击候选字段名一键插入引用
- 公式值仅展示不落库：Grid 单元格、行详情面板、CSV 导出均实时计算；引用为空/无效时显示为空

**数据表 Grid 冻结首列**

- 序号列与主列（名称列）sticky 固定，横向滚动大量字段时保持可见

**Excel/表格剪贴板 TSV 粘贴**

- 从 Excel/WPS/网页表格复制区域后，点击 Grid 中一个起始单元格再 Ctrl+V，数据从锚点向右向下填充
- 行数不够自动新增行；只读字段（公式/最后编辑时间）与超出列数的单元格自动跳过并提示
- 兼容 Excel 剪贴板规范：引号包裹单元格内换行、`""` 转义

### 修复

- 粘贴富文本时 Markdown 列表/任务列表不识别：AI 聊天工具复制的「段落化 markdown」（text/html 为 `<p>- a</p>` 形式）现正确解析为列表
- 图集改为双列网格显示；修复图集 Markdown 导出丢图
- 块拖拽排序后页面滚动位置跳到顶部/底部
- 页面树拖拽排序在 WebView2 下无法触发：拖拽改用鼠标事件链（mousedown/mousemove/mouseup）
- 数学公式块右上角无效的「编辑」按钮移除（双击即可进入编辑）

### 验证

| 命令           | 结果           |
| -------------- | -------------- |
| `tsc --noEmit` | exit 0         |
| `vitest run`   | 177/177 passed |
| `eslint`       | 0 errors       |
| `cargo test`   | 111/111 passed |

---

## v0.6.8 — 2026-09-19

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.6.8_x64-setup.exe`

### 新增

**页面收藏置顶**

- 页面树右键「收藏 / 取消收藏」；收藏的页面在侧边栏顶部收藏区集中显示，点击直达（树中原位置保留）
- 星标图标高亮标识收藏状态

**标签系统**

- 页面右键「编辑标签」：输入新标签回车添加、点击全库已有标签快速勾选 / 移除、× 逐个删除
- 侧边栏顶部标签行：点击标签过滤页面树（保留带标签节点及其祖先路径），再次点击取消过滤
- 标签数据落库（views.tags JSON 列，迁移 009），坏数据静默解析为空不报错

**文档历史版本**

- 页面右键「历史版本」：查看编辑过程中自动保存的快照列表（Rust 侧自动节流落盘，新→旧排序）
- 一键恢复到所选快照；恢复前自动把当前内容存为「恢复前备份」，可再恢复回来

**整库导出 Markdown 文件夹**

- 设置 → 导入导出新增「整库导出 Markdown 文件夹」：按页面树目录结构递归导出全部文档
- 有子页面的页面导出为「同名文件夹 + 同名 .md」，纯叶子页面直接导出 .md
- 文件名自动清理非法字符与 Windows 保留名，同级重名自动加序号；导出后按成功 / 失败分别提示

**斜杠菜单快捷搜索提示**

- 菜单项右侧显示快捷搜索关键词（灰色小字，如公式块显示 `math 公式 $$`），支持关键词与别名过滤，更快定位目标块

### 修复

- 数学公式块右上角无效的「编辑」按钮移除（双击即可进入编辑）
- 导入 Markdown 文件夹无法读取嵌套子目录：目录选择改为递归授权（`recursive: true`）

### 验证

| 命令           | 结果           |
| -------------- | -------------- |
| `tsc --noEmit` | exit 0         |
| `vitest run`   | 167/167 passed |
| `eslint`       | 0 errors       |
| `cargo test`   | 111/111 passed |

---

## v0.6.7 — 2026-09-18

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.6.7_x64-setup.exe`

### 新增

**主题风格预设（6 款，含浅色/深色两套配色）**

- 设置 → 外观新增「主题风格」行，预览卡片所见即所选（跟随当前浅色/深色模式）
- Flowy 蓝（默认）：现有 AppFlowy 风格
- 石墨灰：Notion 式纯净灰调
- 墨绿：温暖纸感 + 绿色
- 紫罗兰：蓝紫晕染
- 日落橙：暖米色调
- 玫瑰粉：柔粉色调

### 改进

- 主题选择与强调色均持久化到 localStorage（此前强调色重启后重置）
- 强调色新增「石板灰」「玫红」两色；手动选择强调色可覆盖主题自带品牌色
- 主按钮颜色跟随强调色，浅色/深色模式一致（此前固定为蓝色）

### 验证

| 命令           | 结果           |
| -------------- | -------------- |
| `tsc --noEmit` | exit 0         |
| `vitest run`   | 164/164 passed |
| `eslint`       | 0 errors       |

---

## v0.6.6 — 2026-09-17

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.6.6_x64-setup.exe`

### 改进

**应用图标去白底，改为透明背景**

- 图标只保留蓝色渐变圆角方块与白色 TF，四周背景完全透明；深色任务栏 / 深色模式下不再出现白色方框与浅色光晕
- 同步重新生成 Windows（icon.ico / icns / 各尺寸 PNG / Store 系列）与 Android、iOS 全套图标
- 透明源图入库为 `src-tauri/icons/icon-source.png`（1024×1024），后续调整图标以它为源重新生成

### 验证

| 命令           | 结果                                                        |
| -------------- | ----------------------------------------------------------- |
| `tsc --noEmit` | exit 0                                                      |
| `vitest run`   | 164/164 passed                                              |
| `eslint`       | 0 errors                                                    |
| `cargo test`   | 106/106 passed                                              |
| `cargo check`  | 0 warnings                                                  |
| 图标嵌入校验   | PE 资源逐字节比对通过（exe 与安装包内嵌图标均为新版透明底） |

---

## v0.6.5 — 2026-09-17

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.6.5_x64-setup.exe`

### 改进

**侧边栏布局调整**

- 新建页面按钮从底部固定区移至搜索框正下方，与搜索框紧贴
- 页面树下移，与新建按钮之间留一个空行的距离
- 底部固定区收缩为「回收站 / 设置」一行，不再为新建按钮留整块占位

### 验证

| 命令           | 结果           |
| -------------- | -------------- |
| `tsc --noEmit` | exit 0         |
| `vitest run`   | 164/164 passed |
| `eslint`       | 0 errors       |
| `cargo test`   | 106/106 passed |
| `cargo check`  | 0 warnings     |

---

## v0.6.4 — 2026-09-15

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.6.4_x64-setup.exe`

### 新功能

**首次启动欢迎文档**

- 标题改为「欢迎使用 TsFlowy」（英文 Welcome to TsFlowy），内容从空页面改为「全部功能示例 + 使用说明」：编辑器标记与斜杠菜单、列表 / 任务 / 有序列表示例、引用代码公式、折叠块、数据库多视图（字段、分组小计、看板、日历、文档内嵌数据库）、搜索 / 命令面板 / 提及 / 大纲与目录块、导出备份与设置、快捷键表、数据保存位置
- 中英双语，按首次启动时的界面语言生成；结构符合编辑器的「首行 H1 + 第二行分割线」锁定，标题与页面名一致
- 文档 JSON 经真实编辑器 schema 单元测试校验（新增 7 个用例），避免首次打开报内容非法

### 文档与构建

- README 重写：功能特性、技术栈、从源码构建（含环境要求、构建产物路径）、使用指南、数据存放位置
- 移除版本库内 `.cargo/config.toml`（含作者本机的构建目录配置，其他机器克隆后会构建到不存在的路径）；个人自定义构建目录可改用 `CARGO_TARGET_DIR` 环境变量

### 验证

| 命令           | 结果           |
| -------------- | -------------- |
| `tsc --noEmit` | exit 0         |
| `vitest run`   | 164/164 passed |
| `eslint`       | 0 errors       |
| `cargo test`   | 106/106 passed |
| `cargo check`  | 0 warnings     |

---

## v0.6.3 — 2026-09-15

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.6.3_x64-setup.exe`

### 新功能

**数据库多视图**

- 同一张数据可以有多个视图（表格 / 看板 / 日历），页面顶栏新增视图标签栏：新建、双击重命名、删除；每个视图保存独立的显示配置（模式、排序、筛选、看板分组字段、日历日期字段），重进页面停回上次使用的视图
- 宿主视图不可删除（双击宿主标签等于重命名页面）；派生视图不进页面树 / 回收站 / 最近访问 / 搜索
- 顶栏左侧即视图标签栏，原来的页面名（与侧边栏、标签页重复）已去掉

**搜索覆盖数据库单元格**

- 单元格此前完全搜不到（单选 / 多选存的是选项 id）；现在按显示文本建索引，搜「进行中」即命中选择了该选项的行
- 点搜索结果直达命中行：自动滚动定位并短时高亮；分组折叠时先展开、日历视图先翻到命中月份；1–2 字短查询补 LIKE 兜底

**表格分组小计与看板卡片字段**

- 表格可按任意字段分组，显示分组小计，底部合计行每列可选汇总函数（字段类型变更自动回落为计数）
- 看板卡片字段改为可勾选，未勾选时沿用「标题后前 3 个可见字段」的默认

**当前页导出**

- 标签栏新增导出菜单：Markdown（先冲刷未保存内容再导出）、长图 PNG、PDF（走系统打印）
- 长图导出会把资产图片内联为 data URL，离线可看

**图片灯箱**

- 图片双击打开灯箱放大，点击切换缩放，Esc 退出

### 改进

**编辑区**

- 隐藏滚动条（滚轮 / 键盘滚动不受影响）
- 光标行不再低于编辑区 70%：打字与键盘导航时自动保持，鼠标点击不触发上滚

**附件打开**

- 附件卡片改用系统默认程序打开（此前点不开）；路径只接受 assets/ 下真实文件，拒绝前缀越权与 `..` 穿越

### 修复

**数据表「+」新建视图菜单不可见** — 下拉被横向滚动容器裁剪

**@提及跳转失效** — Mention 是原子节点，改从 DOM `data-id` 取目标 id 跳转

**图片说明输入重复字符** — 说明框改本地草稿状态

**选中蓝框** — 图片 / 附件 / 图集选中时的蓝色描边移除

**列统计栏** — 去掉多余上边框

### 移除功能

- `/` 菜单移除多栏布局（columns2 / 3 / 4）
- 移除从未接线的 Daily Notes 入口与模板残留 i18n 键

### 验证

| 命令           | 结果           |
| -------------- | -------------- |
| `tsc --noEmit` | exit 0         |
| `vitest run`   | 157/157 passed |
| `eslint`       | 0 errors       |
| `cargo test`   | 106/106 passed |
| `cargo check`  | 0 warnings     |

---

## v0.6.2 — 2026-09-12

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.6.2_x64-setup.exe`

### 改进

**首行一级标题内容可编辑，并作为文档标题**

- 第一行固定为一级标题（不可删除 / 转换 / 改级别 / 拖拽排序），但标题内容现在可以直接在编辑区内修改
- 标题即文档标题：在编辑区修改 H1 内容会自动同步为页面名称（侧边栏树 / 标签页联动）；在侧边栏重命名页面也会反向写回 H1
- H1 内回车不再把标题一分为二，改为光标跳到下一内容块（没有内容块时新建空段落）
- 第二行分割线仍保持不可修改

### 验证

| 命令           | 结果           |
| -------------- | -------------- |
| `tsc --noEmit` | exit 0         |
| `vitest run`   | 128/128 passed |
| `eslint`       | 0 errors       |

---

## v0.6.1 — 2026-09-12

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.6.1_x64-setup.exe`

### 修复

**侧边栏「大纲」功能失效**

- 根因：编辑器实例创建后未推送到全局 store（`useEditorStore.setEditor` 从未被调用），大纲组件拿到的 editor 恒为 null，标题列表始终为空
- 修复：EditorPage 挂载时推送 editor 到全局 store、卸载/切换视图时清空，大纲恢复收集 H1/H2/H3、可视区高亮与点击跳转

**光标在最后一行时悬浮菜单内容超出可视区**

- 根因：选中文本浮动工具栏贴近编辑区底部时，点击「高亮 / 颜色 / 链接」后弹层默认向下展开，超出可视区底部导致看不到内容（块菜单的调色板同理）
- 修复：弹层根据剩余空间自动翻转——下方空间不足且上方空间更大时改为向上展开

### 移除功能

**取消「收藏」功能**

- 移除侧边栏「收藏」Tab、页面树右键菜单的「收藏 / 取消收藏」项、前端 workspace store / db 的收藏相关状态与 API，以及 Rust 端 `view_list_favorites` / `view_set_favorite` 命令与 `list_favorites` / `set_favorite` 函数
- 数据库 `is_favorite` 列保留（避免数据迁移风险），存量数据不受影响

### 验证

| 命令           | 结果           |
| -------------- | -------------- |
| `tsc --noEmit` | exit 0         |
| `vitest run`   | 123/123 passed |
| `eslint`       | 0 errors       |
| `cargo check`  | exit 0         |

---

## v0.6.0 — 2026-09-11

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.6.0_x64-setup.exe`

### 改进

**浮动窗口限制在编辑区范围内**

- 选中文本浮动工具栏、块菜单、表格右键菜单等浮动窗口，坐标会限制在编辑区可视矩形内，不再飘出编辑区
- 修复行详情窄面板 / 编辑区宽度调窄等场景下，浮层跑到编辑区之外的问题

**数据表行详情页不再显示「添加属性」**

- 行详情页的正文编辑区隐藏「添加属性」区（属性已在面板顶部编辑，无需在正文重复）

### 修复

- 顺带修复此前数据库 WAL 日志损坏导致应用无法启动的问题（移除损坏 WAL，基于健康主库重建）

---

## v0.5.0 — 2026-09-11

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.5.0_x64-setup.exe`

### 新功能

**编辑区块拖拽排序**

- 编辑器左侧悬停显示拖拽手柄，按住可上下拖动块调整顺序，拖动时显示蓝色插入指示条，松开即落位
- 重写为鼠标事件链实现（原先 HTML5 拖拽事件在部分环境下不可靠，排序无法触发）

**块拖拽手柄点击菜单**

- 点击手柄（不拖动）弹出块操作菜单：
  - 对齐方式：左 / 中 / 右
  - 转换块类型：文本、标题 1-3、引用、代码块、项目列表、有序列表、任务列表；容器块（列表/引用/表格等）提供「转为文本」解包，且不会产生多余空段落
  - 剪切 / 复制 / 删除
  - 文字颜色 / 背景色（与选中文本浮动工具栏共用调色板）

---

## v0.4.0 — 2026-09-11

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.4.0_x64-setup.exe`

### 新功能

**行详情页放大显示**

- 行详情面板头部新增放大按钮：点击将行详情展开为大尺寸视图并在屏幕中央居中显示（90vh × min(92vw, 1100px)），再次点击回到右侧侧栏

**编辑区宽度可调**

- 设置页「外观」区新增「编辑区宽度」：窄 640 / 默认 800 / 宽 960 / 超宽 1120px，全屏时选宽档即可让正文更舒展；即时生效并记住选择（沿用字号/行距的 CSS 变量 + localStorage 方案）

### 修复

- 修复数据表行详情页单/多选字段无法添加选项：编辑容器缺少 `relative` 定位，导致下拉与「添加选项」输入框锚定到屏幕外不可见；现已与数据表网格一致，正确锚定在字段下方

---

## v0.3.1 — 2026-09-05

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.3.1_x64-setup.exe`

### 新功能

**字段/属性「直接新建 + 点图标改类型」**

- 数据表行详情与页面属性的添加按钮不再弹出名称/类型对话框，点击即新建一行文本型字段/属性（默认名自动去重：「字段」「字段 2」…「属性」「属性 2」…）

- 点击字段图标即可更换类型：行详情支持全部 11 种字段类型，页面属性支持 6 种属性类型；改类型沿用列头菜单语义（单选/多选选项重置、单元格值清空）

- 行详情字段名称支持点击行内重命名（Enter 落库 / Esc 取消）

**搜索短词优化**

- 1-2 字查询现在能命中文档正文：trigram 全文索引无法匹配短词，现自动回退到正文 LIKE 匹配（含通配符转义，跨空间结果不串）

### 性能

- 列宽拖拽改为本地预览：拖拽期间直改列宽零渲染零 IPC，松手一次落库（原来每像素写库）

- 页面属性值输入 500ms 防抖落库，失焦/切页立即冲刷

- 侧栏页面树 / 标签栏 / 行详情 / 回收站等 6 个组件改 zustand 原子订阅，消除全局 store 变化引发的整树重渲

### 修复

- 回收站、空间切换、页面树、搜索等约 13 处静默失败补报错提示（toast）

- 移除未使用的 RecentTab 组件；shadcn 依赖移至 devDependencies

---

## v0.3.0 — 2026-09-05

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.3.0_x64-setup.exe`

### 架构：持久层全量下沉 Rust（P1 完结）

tauri-plugin-sql 彻底移除（Rust 依赖 / lib.rs 插件注册 / capabilities sql 权限 / 前端 npm 包），全部持久化改为 Rust rusqlite 类型化领域命令：

- 新增 `src-tauri/src/db/`（mod / migrations / models / workspaces / views / settings / docs / mentions / search / properties / database）+ `commands/db.rs` 薄包装

- Db = 单写 + 单读 Mutex<Connection>：写天然串行，前端全局 JS 写锁（withWriteLock / runInTransaction）整体删除，跨连接事务溶解问题根除

- 迁移自管 `schema_migrations`（含 sqlx 基线检测，存量库不重跑 001-006）；`_sqlx_migrations` 成为惰性遗留

- 前端 API 签名不变（workspaceApi / viewApi / documentApi / databaseApi / pagePropertiesApi / searchApi / mentionsApi），stores 零改动

- 备份导出 / 导入 / 重置数据目录改为 close+reopen bracket，应用运行中即可完成（不再要求先关应用）

**批量操作单命令化**

- CSV 导入改 `csv_import` 单命令：一个事务落全部字段/行/单元格，消灭原来每行 1+N 次 IPC 的 N+1

- 字段重排 / 行重排（reorder）改 `unchecked_transaction` 单事务批量 UPDATE

### 新功能

**页面属性移到标题下方**

- 文档首个 H1 之后内联渲染属性区（ProseMirror widget + React portal），样式对齐数据表行详情：字段图标 + 名称（点击重命名）+ 值 + hover 删除

- 支持添加 text / date / single_select / multi_select / number / checkbox 六类属性

**文档 emoji 图标**

- 点击页面树条目的图标本身弹出选择器（30 emoji + 清除图标，选中后不自动关、可连续切换），实时同步侧栏树 / 标签页 / 面包屑并落库

- 空间名称前的 emoji 显示取消（数据保留，仅去掉展示）

### 移除功能

**侧栏「文档日历」组件**（DocCalendar.tsx 删除；数据表网格视图的 网格/看板/日历 模式切换保留）

### Bug 修复

- 数学公式 / 折叠块导出丢失（markdown 导出补 $$ 围栏与 <details> 嵌套解析，附往返等值测试）

- 关窗时最后编辑可能丢失：close-flush 注册表 + onCloseRequested 统一冲刷（5s 看门狗）

- 加载竞态覆盖整篇文档：编辑器加载完成前保持只读，加载内容不再触发自动保存

- export_backup 缺 WAL 数据：先 VACUUM INTO 一致性快照再打包（旧方式已实证整表丢失）

- 恢复备份不清理旧 -wal/-shm：stash 时 db+wal+shm 一并改名，孤儿侧车清理 + 回滚还原

### 验证

| 命令           | 结果                                                           |
| -------------- | -------------------------------------------------------------- |
| `tsc --noEmit` | exit 0                                                         |
| `vitest run`   | 107/107                                                        |
| `cargo test`   | 78/78                                                          |
| CDP 实测       | 文档 / 表格 / CSV / 页面属性 / 图标全链路 + 冷启动，零 JS 错误 |

## v0.2.2 — 2026-09-04

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.2.2_x64-setup.exe`

### Bug 修复

**database is locked (code 5) 根治**

- 根因：上轮 `_writeDepth` 可重入方案有并发漏洞。JS 模块级变量无法区分"同栈嵌套"与"不同操作并发"——saveNow 的 fn 在 await 期间让出执行权，另一个写操作看到 `_writeDepth=1` 误判可重入直接执行 → 并发写 → SQLITE\_BUSY

- 修复：移除 `_writeDepth`，恢复纯串行锁；`mentions.rebuildFor` 移出 `saveNow` 的 `withWriteLock` 块，避免嵌套调用 `runInTransaction → withWriteLock` 死锁。rebuildFor 锁外自己加锁，派生索引允许短暂异步滞后于文档内容

### 移除功能

**取消数据表"关联"字段类型（relation）**

- 移除：`FieldType`/`FIELD_TYPES` 的 relation、`FieldOptions` 的 relation kind、`CellEditorSlot` 的 relation 分支、`RelationCellEditor` 组件、`RelationTargetPicker`、`relation.ts` 文件、`database-values`/`database-query`/`BoardView`/`RowDetail` 的 relation 处理、i18n relation 文案

- 测试同步清理 relation 用例（净减 108 行）

### 验证

| 命令                  | 结果                |
| --------------------- | ------------------- |
| `tsc --noEmit`        | exit 0              |
| `vitest run`          | 97/97 passed        |
| `npm run lint`        | 0 errors            |
| `npm run tauri build` | NSIS 安装包生成成功 |

---

## v0.2.1 — 2026-09-04

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.2.1_x64-setup.exe`

### Bug 修复

**1. 关联字段输入框弹出后页面卡死**

- 根因：`RelationCellEditor` 底层全屏遮罩 `onMouseDown={() => undefined}` 不关闭，遮罩永久挡住整个视口无法操作

- 修复：改为 `onCommit([...selected])`，点击外部提交当前选中并关闭（与 MultiSelectCellEditor 的 `commit()` 机制一致）

**2. database is locked (code 5)**

- 根因：`withWriteLock` 不可重入。`documents.saveNow` 在 withWriteLock 内调用 `mentions.rebuildFor` → `runInTransaction` → `withWriteLock`，内层等外层 fn 完成、外层 fn 等内层完成 → Promise 死锁

- 修复：加入 `_writeDepth` 可重入计数，检测到已在写锁内则直接执行不再排队

**3. 新建页面重命名后切页恢复"未命名"**

- 根因：`viewApi.rename` 等所有写操作裸 `d.execute` 不走 withWriteLock，与并发写冲突触发 SQLITE\_BUSY → rename 静默失败 → 名字没落库也没更新本地 tree；`commitRename` 的 try/catch 吞掉异常用户无感知

- 修复：viewApi 全部写操作（touchVisited/create/rename/setIcon/setFavorite/softDelete/restore/purge/purgeTrash/purgeExpiredTrash）包 withWriteLock；commitRename 失败时 toast 提示不再静默吞掉

### 验证

| 命令                  | 结果                |
| --------------------- | ------------------- |
| `tsc --noEmit`        | exit 0              |
| `vitest run`          | 97/97 passed        |
| `npm run lint`        | 0 errors            |
| `npm run tauri build` | NSIS 安装包生成成功 |

---

## v0.2.0 — 2026-09-04

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.2.0_x64-setup.exe`

### 新增功能

**编辑器体验**

- 反链面板：文档底部按来源分组显示反向链接，可收起；切页自动刷新

- mention hover 预览：鼠标悬停 `@提及` 300ms 后浮层显示目标文档前 200 字纯文本（带缓存，避免重复请求）

- PageProperties 接入编辑器顶部：标题下方横排 chip 行，支持 text/date/single\_select/multi\_select/number/checkbox 六种类型

- 图片粘贴上传：粘贴剪贴板图片自动写入 `$APPDATA/assets/` 并插入 Image 节点

- 块拖拽：`BlockDrag` TipTap 扩展，hover 显示拖拽手柄 + 蓝色 drop 指示

- 字数统计：右下角浮层实时显示字符数

**侧边栏**

- 三 Tab 切换：页面树 / 大纲 / 收藏（固定三项，不可删减）

- 新建页面按钮下移至底部固定区，回收站/设置上方

- 文档日历：内容区下方挂载 `DocCalendar`，默认折叠，展开后按月聚合最近编辑文档，点格跳转/创建当日笔记

- 移除 Breadcrumb 组件

**设置**

- 字号设置：小 14 / 中 16 / 大 18 / 特大 20，写入 `--tiptap-font-size` CSS 变量即时生效

- 行距设置：紧凑 1.5 / 正常 1.75 / 宽松 2.0，写入 `--tiptap-line-height`

### 导入导出体系完善

- 单页 Markdown 导出复用 `markdown.ts` 的 `jsonToMarkdown`：支持 callout/toggle/columns/math/gallery/database-view/attachment 等高级块，输出可读占位注释不丢结构

- HTML 导出补 `escapeAttr()`：img src / a href / code class 全转义，防 XSS 与标签破坏

- 导入 Markdown 文件夹：选目录 → 递归遍历 `.md` → 按文件夹层级建页（子目录建父页，`.md` 作子页）→ 页名优先取首个 H1 → 内容走 `markdownToJson` → `reload` 刷树

- 导入后按 created/failed 分别 toast 提示

### 验证

| 命令                  | 结果                                 |
| --------------------- | ------------------------------------ |
| `tsc --noEmit`        | exit 0                               |
| `vitest run`          | 97/97 passed                         |
| `npm run lint`        | 0 errors                             |
| `lint-staged`         | eslint --fix + prettier --write 通过 |
| `npm run tauri build` | NSIS 安装包生成成功                  |

---

## v0.1.0 — 2026-09-04

### 安装包

Windows x64 NSIS 安装包：`TsFlowy_0.1.0_x64-setup.exe`（5.34 MB）

### 新增功能

**P0 · 数据库与性能**

- SQLite WAL 模式 + `synchronous=NORMAL` + busy\_timeout=5000ms + cache\_size=-10000（PRAGMA 性能优化）

- 006 复合索引：mentions 双向查询、workspace views/favorites/trash 三视角显著加速

- Tauri capabilities 最小化：fs 权限收窄至 `allow-appdata-write-recursive` 等 4 条

- `runInTransaction` 事务工具：moveView/renumber 等批量重排操作具备原子性

**P1 · 前端体验**

- TabBar 状态持久化：tabs / 当前 view 写入 `app_settings`，重启原样恢复（含 debounce）

- 19 组件 Zustand 选择器：`useShallow`/原子选择器替换对象字面量，避开 React 19 useSyncExternalStore 缓存告警

- TypeScript strict：floating-menu / block-drag 等 4 处 `any` 全部移除

- ESLint + Prettier：0 errors，215 warnings 留待后续渐进清理

- Husky + lint-staged：pre-commit 自动 lint 修复

- logger 门面：`logger.trace/debug/info/warn/error/catch`，生产默认 warn 级别，`console.*` 裸写清零

**P2 · 性能与健壮性**

- Document save mailbox：快速编辑合并成单次 DB 写入，避免频繁 IO

- mentions backfillIfEmpty 分批：8 docs/tick + 10ms sleep，大库启动不卡 UI

- fallback 反链扫描节流：30min 全局窗口，避免打开每篇文档都全库扫描

- moveView / renumber 事务化：崩溃不会出现父子/position 半刷不一致

**新接入的功能**

1. 首 Heading 锁定文档名：`FirstHeadingLock` TipTap 扩展 — 文档第一 H1 文本自动同步到 `view.name`
2. SubPagePicker：顶栏「+ 子页面」按钮 → 弹出选择器 → 插入 TipTap SubPage node
3. Sidebar 最近 / 收藏 Tab：一级 Tab 切换（页面树 / 最近+收藏），二级 Tab（最近访问 / 收藏夹）
4. Ctrl+Shift+F 全局搜索（Settings 快捷键表已列出，App.tsx 统一注册）
5. 数据库字段类型恢复：`FIELD_TYPES` 补齐 `relation / created_at / last_edited_at`

**UI 精简**

- Breadcrumb 只显示祖先链，当前页不再重复展示

- EditorPage/GridView/BoardView/CalendarView 4 处顶栏删除与 TabBar 重复的 `viewIcon`

### Bug 修复

- React 19 DevMode 死循环（`getSnapshot should be cached`）：4 条原子 selector + store 内部 ready 兜底 + useShallow 统一治理

- tauri.conf.json 非法字段清理：`app.updater`、NSIS `allowDowngrades` 等 4 条、`headerImage/sidebarImage` 空字符串

- `capabilities/default.json` 4 条 fs 权限名拼写修正

### 构建 / CI

- GitHub Actions Windows CI：`tsc → vitest → cargo check` 三阶段

- NSIS 打包：简体中文单语言，Tauri 默认命名 `TsFlowy_{version}_x64-setup.exe`（含版本号）

### 验证

| 命令           | 结果                    |
| -------------- | ----------------------- |
| `tsc --noEmit` | exit 0                  |
| `vitest run`   | 97/97 passed            |
| `npm run lint` | 0 errors / 215 warnings |
| `cargo check`  | 0 warnings / 0 errors   |
